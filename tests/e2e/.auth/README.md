Auth state files are written here by the Playwright `setup-auth` project.
The setup flow authenticates through the dev-only `/auth/test-login` route and saves request-context storage state for each seeded role.
They are generated locally and ignored by Git.
