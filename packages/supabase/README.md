# Supabase Workspace

This package will own:

- local `supabase` CLI config
- SQL migrations
- seed data
- generated database types
- storage bucket conventions
- row-level security policies

Phase 2 keeps auth local-first with magic link as the first real path and phone OTP as a placeholder UX path.
