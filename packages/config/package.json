{
  "name": "@glowhaul/config",
  "private": true,
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "lint": "echo \"Config lint not wired yet\"",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "tsc --project tsconfig.test.json && node --test .tmp-test/index.test.js"
  },
  "dependencies": {
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.8.3"
  }
}
